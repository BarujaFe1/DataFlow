name: Keep Render Backend Warm

on:
  schedule:
    - cron: "*/14 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping health endpoint
        run: curl -s -o /dev/null -w "%{http_code}" https://dataflow-glu1.onrender.com/api/health
