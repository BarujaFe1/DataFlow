# Development / CI dependencies for the DataFlow backend API.
# Pulls in the runtime set via -r requirements.txt, then adds tooling.
# Install with: pip install -r requirements-dev.txt -c constraints.txt
-r requirements.txt

# ---- Test + lint gate (CI enforces these) ----
pytest>=8.0.0
pytest-cov>=5.0.0
ruff>=0.5.0

# Starlette's TestClient (used by the test suite) requires httpx.
# This was previously installed only ad-hoc in the local env, which is exactly
# why the clean CI runner failed with:
#   "RuntimeError: The starlette.testclient module requires the httpx package"
httpx>=0.27.0
