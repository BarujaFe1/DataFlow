// Extend Vitest's expect with DOM matchers (toBeInTheDocument, toHaveAttribute...).
// The `/vitest` subpath augments Vitest's own `expect` (types + runtime).
import "@testing-library/jest-dom/vitest";
